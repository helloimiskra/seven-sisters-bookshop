RSpec.describe Seven::Sisters::Bookshop do
  it "has a version number" do
    expect(Seven::Sisters::Bookshop::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
